# ReactJS - pierwszy komponent, wyświetlanie komponentów reactowych na stronie

- dodajemy plik `react.html` do `WebContent`
- dodajemy pliki z katalogu `react-src` do katalogu `react-src` w projekcie
