# ReactJS - routowanie w modelu SPA i wykorzystanie RESTful poprzez AJAX

- Należy przenieść wszystkie pliki z tego katalogu (`presentional/`, `containers/`, `app.js` i `api.js`) do katalogu `react-src` w naszym projekcie
